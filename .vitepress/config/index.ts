import { defineConfig } from "vitepress";

export default defineConfig({
  title: "VitePress Versions",
  description: "Showcase of VitePress Versions with folders.",
  themeConfig: {
    socialLinks: [
      { icon: "github", link: "https://github.com/its-miroma/vpv-folder" },
    ],
  },
});
