import { defineConfig } from "vitepress";

export default defineConfig({
  title: "VitePress Versions - V3",
  description: "V3 of the showcase of VitePress Versions with folders.",
  themeConfig: {
    socialLinks: [
      { icon: "github", link: "https://github.com/its-miroma/vpv-folder" },
    ],
  },
});
